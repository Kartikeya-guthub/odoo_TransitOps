feat(maintenance,fuel): add maintenance workflow and fuel/expense logging

- add maintenance create/close with automatic vehicle status trigger 
  (IN_SHOP on create, AVAILABLE on close unless RETIRED)
- add vehicle retire action, guarded against ON_TRIP vehicles
- add ad-hoc fuel log and expense entry, independent of trip completion, 
  usable against any vehicle regardless of status
- invalidate vehicle/available-pool queries on maintenance changes

Closes all vehicle status transitions and completes the raw data needed 
for cost/efficiency/ROI reporting.
